// AUTO-GENERATED by scripts/build.mjs — do not edit.
window.PHASES = [
  {
    "id": 0,
    "title": "前置環境",
    "weeks": "1 週",
    "difficulty": "2",
    "lessons": [
      {
        "id": "0",
        "name": "前置環境",
        "status": "complete"
      }
    ],
    "goal": "能跑 ROS 2,不卡環境。"
  },
  {
    "id": 1,
    "title": "ROS 2 核心通訊",
    "weeks": "3-4 週",
    "difficulty": "2→3",
    "lessons": [
      {
        "id": "1-1",
        "name": "骨架",
        "status": "complete"
      },
      {
        "id": "1-2",
        "name": "Topics",
        "status": "complete"
      },
      {
        "id": "1-3",
        "name": "Services + Interfaces",
        "status": "complete"
      },
      {
        "id": "1-4",
        "name": "工程化",
        "status": "complete"
      }
    ],
    "goal": "掌握節點如何溝通。ROS 2 主體。"
  },
  {
    "id": 2,
    "title": "進階通訊 + 除錯",
    "weeks": "2 週",
    "difficulty": "3",
    "lessons": [
      {
        "id": "2",
        "name": "進階通訊 + 除錯",
        "status": "planned"
      }
    ],
    "goal": "補齊入門課常漏但核心的部分。"
  },
  {
    "id": 3,
    "title": "空間與模型",
    "weeks": "3-4 週",
    "difficulty": "4",
    "lessons": [
      {
        "id": "3-1",
        "name": "tf2",
        "status": "planned"
      },
      {
        "id": "3-2",
        "name": "空間數學",
        "status": "planned"
      },
      {
        "id": "3-3",
        "name": "URDF",
        "status": "planned"
      }
    ],
    "goal": "從\"程式\"進化到\"機器人\"。**這階段是分水嶺。**"
  },
  {
    "id": 4,
    "title": "模擬與運動",
    "weeks": "4-5 週",
    "difficulty": "4→5",
    "lessons": [
      {
        "id": "4-1",
        "name": "物理模擬",
        "status": "planned"
      },
      {
        "id": "4-2",
        "name": "選一條深入",
        "status": "planned"
      }
    ],
    "goal": "純軟體跑完整機器人邏輯。"
  },
  {
    "id": 5,
    "title": "AI 整合",
    "weeks": "3-4 週",
    "difficulty": "3",
    "lessons": [
      {
        "id": "5",
        "name": "AI 整合",
        "status": "planned"
      }
    ],
    "goal": "發揮 AI/SWE 優勢,把 LLM/視覺接進機器人。"
  },
  {
    "id": 6,
    "title": "收尾/深化",
    "weeks": "持續",
    "difficulty": "5",
    "lessons": [
      {
        "id": "6",
        "name": "收尾/深化",
        "status": "planned"
      }
    ]
  }
];
window.DOCS = [{"id":"0/01-environment","phase":0,"order":1,"slug":"01-environment","title":"環境建置（Docker + ROS 2）","blurb":"用 Docker 起乾淨的 ROS 2 Jazzy 環境，跑通內建 talker/listener 兩節點對話，確認通訊活著。","html":"<h1>環境建置（Docker + ROS 2）</h1>\n<blockquote>用 Docker 起乾淨的 ROS 2 Jazzy 環境，跑通內建 talker/listener 兩節點對話，確認通訊活著。</blockquote>\n<h3>概念</h3>\n<p>純軟體起步，0 硬體。用 Docker 跑 ROS 2 Jazzy（2024 LTS），環境乾淨可丟，不卡系統。掛載 host 目錄 → 檔案留在 Mac 上。</p>\n<h3>Step 1: 拉 ROS 2 Jazzy image</h3>\n<pre><code>docker pull osrf/ros:jazzy-desktop</code></pre>\n<ul><li>desktop 版含 RViz/rqt/turtlesim GUI 工具 (Phase 1 要用)</li><li>~1-2GB,拉一次。arm64 自動選對</li><li>預期: 一堆 Pull complete → Status: Downloaded</li></ul>\n<p>驗證:</p>\n<pre><code>docker images | grep jazzy</code></pre>\n<p>預期: 看到 osrf/ros jazzy-desktop</p>\n<h3>Step 2: 建持久 workspace (host 端)</h3>\n<p>容器內檔案關掉會丟。掛載 host 目錄 → 檔案留 Mac 上。</p>\n<pre><code>mkdir -p ~/Workspace/projects/robotics-engineering-from-scratch/ros2_ws/src</code></pre>\n<ul><li>ros2_ws = 你的工作空間</li><li>src = 放 package 原始碼處 (ROS 慣例)</li></ul>\n<h3>Step 3: 起容器</h3>\n<pre><code>docker run -it --rm \\\n  --name ros2_dev \\\n  -v ~/Workspace/projects/robotics-engineering-from-scratch/ros2_ws:/root/ros2_ws \\\n  osrf/ros:jazzy-desktop \\\n  bash</code></pre>\n<p>拆解:</p>\n<ul><li>-it = 互動終端</li><li>--rm = 關掉自動刪容器 (檔案在掛載目錄,不怕)</li><li>--name ros2_dev = 容器命名</li><li>-v host路徑:容器路徑 = 掛載。你 Mac 的 ros2_ws ↔ 容器 /root/ros2_ws</li><li>最後 bash = 進 shell</li></ul>\n<p>預期: prompt 變 root@容器id:/# → 你在容器內了</p>\n<h3>Step 4: 容器內驗證 ROS 2</h3>\n<p>進容器後:</p>\n<pre><code>source /opt/ros/jazzy/setup.bash</code></pre>\n<ul><li>載入 ROS 2 環境。每開新 shell 都要 source (最常忘,命令找不到就是這)</li></ul>\n<pre><code>ros2 --help</code></pre>\n<p>預期: 一堆子命令 (run, topic, node, service...)</p>\n<h3>Step 5: 驗收 — talker/listener</h3>\n<p>ROS 2 內建 demo。兩節點對話 = 通訊活著。</p>\n<p>Terminal 1 (現有容器內):</p>\n<pre><code>source /opt/ros/jazzy/setup.bash\nros2 run demo_nodes_cpp talker</code></pre>\n<p>預期: Publishing: 'Hello World: 1' 每秒一行</p>\n<p>Terminal 2 — 新 Mac 終端,進同容器:</p>\n<pre><code>docker exec -it ros2_dev bash</code></pre>\n<p>容器內:</p>\n<pre><code>source /opt/ros/jazzy/setup.bash\nros2 run demo_nodes_cpp listener</code></pre>\n<p>預期: I heard: [Hello World: 1] — 收到 talker 發的</p>\n<p>兩邊數字對上 = Phase 0 通過。 🎯</p>\n<p>停止: 各 terminal Ctrl+C。</p>\n<h3>常見坑</h3>\n<pre><code>┌─────────────────────────┬──────────────────────┬──────────────────────────────────┐\n│          症狀           │         原因         │                解                │\n├─────────────────────────┼──────────────────────┼──────────────────────────────────┤\n│ ros2: command not found │ 沒 source            │ source /opt/ros/jazzy/setup.bash │\n├─────────────────────────┼──────────────────────┼──────────────────────────────────┤\n│ listener 沒收到         │ 沒同容器 / 沒 source │ 用 docker exec 進同一 ros2_dev   │\n├─────────────────────────┼──────────────────────┼──────────────────────────────────┤\n│ pull 很慢               │ image 大             │ 等,或換網路                      │\n└─────────────────────────┴──────────────────────┴──────────────────────────────────┘</code></pre>"},{"id":"1/01-package","phase":1,"order":1,"slug":"01-package","title":"第一個 Package","blurb":"Package 是 ROS 最小專案單位，從建立、編譯到寫出第一個繼承 Node 的真節點。","html":"<h1>第一個 Package</h1>\n<blockquote>Package 是 ROS 最小專案單位，從建立、編譯到寫出第一個繼承 Node 的真節點。</blockquote>\n<p>概念: Package = ROS 最小專案單位 (= npm package / python module)。所有 node 住在 package 裡。</p>\n<h3>Step 1: 進容器 + 到 src</h3>\n<p>docker exec -it ros2_dev bash容器內:source /opt/ros/jazzy/setup.bashcd /root/ros2_ws/src</p>\n<h3>Step 2: 建 Python package</h3>\n<p>ros2 pkg create --build-type ament_python --node-name my_first_node my_py_pkg</p>\n<h3>拆解:</h3>\n<ul><li>pkg create = 建 package</li><li>--build-type ament_python = Python 版 (C++ 用 ament_cmake,晚點)</li><li>--node-name my_first_node = 順便建一個範例 node</li><li>my_py_pkg = package 名</li></ul>\n<p>預期: going to create a new package + 一串建檔訊息</p>\n<h3>Step 3: 看結構</h3>\n<p>ls -R /root/ros2_ws/src/my_py_pkg</p>\n<p>預期結構:</p>\n<pre><code>my_py_pkg/\n├── package.xml          # 元資料 + 依賴 (= package.json)\n├── setup.py             # 建置設定 (= setup.py)\n├── setup.cfg\n├── resource/\n└── my_py_pkg/\n    └── my_first_node.py # 你的 node 程式碼</code></pre>\n<h3>看懂對照:</h3>\n<pre><code>┌──────────────────┬──────────────────────────────────────┐\n│        檔        │                 類比                 │\n├──────────────────┼──────────────────────────────────────┤\n│ package.xml      │ 依賴宣告 (package.json 的 deps)      │\n├──────────────────┼──────────────────────────────────────┤\n│ setup.py         │ 建置+進入點 (entry_points 註冊 node) │\n├──────────────────┼──────────────────────────────────────┤\n│ my_first_node.py │ node 本體                            │\n└──────────────────┴──────────────────────────────────────┘</code></pre>\n<h3>Step 4: 編譯</h3>\n<p>回 workspace 根 (不是 src):cd /root/ros2_wscolcon build</p>\n<ul><li>colcon = ROS 2 建置工具 (= make/cargo)</li><li>一定在 ws 根跑,不在 src</li></ul>\n<p>預期: Finished &lt;&lt;&lt; my_py_pkg + Summary: 1 package finished</p>\n<h3>Step 5: source + 跑</h3>\n<p>source /root/ros2_ws/install/setup.bashros2 run my_py_pkg my_first_node</p>\n<ul><li>這次 source 的是你的 ws (不是 /opt/ros) → 才找得到你的 package</li></ul>\n<p>預期: Hi from my_py_pkg.</p>\n<h3>驗收: 看到 Hi from my_py_pkg. = Phase 1.1 過。</h3>\n<p>跑完回報。過了 → 拆解 node 程式碼 (真正懂 ROS node 怎麼寫),再進 1.2 Topics。</p>\n<h3>記憶點 (兩個 source)</h3>\n<p>source /opt/ros/jazzy/setup.bash        # ROS 本體 (開 shell 必做)source /root/ros2_ws/install/setup.bash # 你的 package (build 後必做)每次 colcon build 後要重 source 第二行。</p>\n<p>現在拆解 node 程式碼 — 懂 ROS node 骨架,才寫得出真的。</p>\n<h3>看產生的 node</h3>\n<p>cat /root/ros2_ws/src/my_py_pkg/my_py_pkg/my_first_node.py</p>\n<p>預設長這樣 (簡陋):def main():    print('Hi from my_py_pkg.')</p>\n<p>if __name__ == '__main__':    main()</p>\n<p>這不是真 node — 只是 print。真 ROS node 要繼承 Node 類。</p>\n<h3>改寫成真 node</h3>\n<p>用容器內編輯器,或直接在 Mac 上改 (檔案掛載著,路徑):~/Workspace/tutorial/robotics-engineering-from-scratch/ros2_ws/src/my_py_pkg/my_py_pkg/my_first_node.py</p>\n<p>貼這份:</p>\n<pre><code>import rclpy\nfrom rclpy.node import Node\n\n\nclass MyFirstNode(Node):\n    def __init__(self):\n        super().__init__(\"my_first_node\")   # node 名字\n        self.counter_ = 0\n        self.get_logger().info(\"Node started.\")\n        self.create_timer(1.0, self.timer_callback)  # 每 1 秒呼叫一次\n\n    def timer_callback(self):\n        self.counter_ += 1\n        self.get_logger().info(f\"Hello {self.counter_}\")\n\n\ndef main(args=None):\n    rclpy.init(args=args)          # 啟動 ROS 通訊\n    node = MyFirstNode()           # 建 node\n    rclpy.spin(node)               # 保持運行,處理 callback (= event loop)\n    rclpy.shutdown()               # 收尾\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre>\n<h3>骨架拆解 (記這 5 塊)</h3>\n<pre><code>┌────────────────────────┬─────────────────────────┬───────────────────────────┐\n│           塊           │          作用           │           類比            │\n├────────────────────────┼─────────────────────────┼───────────────────────────┤\n│ rclpy.init()           │ 啟動 ROS client library │ 連線初始化                │\n├────────────────────────┼─────────────────────────┼───────────────────────────┤\n│ class(Node)            │ 繼承 Node = 你的 node   │ 繼承 framework base class │\n├────────────────────────┼─────────────────────────┼───────────────────────────┤\n│ super().__init__(\"名\") │ 註冊 node 名            │ 服務註冊                  │\n├────────────────────────┼─────────────────────────┼───────────────────────────┤\n│ create_timer           │ 週期性觸發              │ setInterval / cron        │\n├────────────────────────┼─────────────────────────┼───────────────────────────┤\n│ rclpy.spin()           │ 卡住+跑 callback        │ event loop (Node.js 那種) │\n└────────────────────────┴─────────────────────────┴───────────────────────────┘</code></pre>\n<p>關鍵心智: ROS node = 事件驅動。spin() 是 event loop,callback 是事件處理器。你 SWE 熟。</p>\n<h3>重新 build + 跑</h3>\n<p>cd /root/ros2_wscolcon buildsource install/setup.bashros2 run my_py_pkg my_first_node</p>\n<p>預期:[INFO] [...] [my_first_node]: Node started.[INFO] [...] [my_first_node]: Hello 1[INFO] [...] [my_first_node]: Hello 2...每秒一行遞增。Ctrl+C 停。</p>\n<h3>小技巧: 免每次 rebuild</h3>\n<p>Python node 可用 symlink 模式,改碼免重 build:colcon build --symlink-install建一次後,改 .py 直接 ros2 run 生效 (省時)。</p>\n<h3>驗收: 看到 Hello 1, 2, 3... 遞增 = 你寫出第一個真 node。</h3>"},{"id":"1/02-topics","phase":1,"order":2,"slug":"02-topics","title":"Topics（發布/訂閱）","blurb":"用 Publisher/Subscriber 打通 ROS 最核心的 Topic 通訊，一發一收即時串流資料。","html":"<h1>Topics（發布/訂閱）</h1>\n<blockquote>用 Publisher/Subscriber 打通 ROS 最核心的 Topic 通訊，一發一收即時串流資料。</blockquote>\n<h3>概念</h3>\n<p>Topic = pub/sub 通道 (= Kafka/MQTT)。</p>\n<ul><li>Publisher 發訊息到 topic (不管誰收)</li><li>Subscriber 訂 topic 收訊息 (不管誰發)</li><li>多對多、非同步、單向串流</li><li>用途: sensor 資料、影像、速度指令 — 90% ROS 通訊</li></ul>\n<pre><code>[Publisher] --/topic--&gt; [Subscriber]\n   node A                  node B</code></pre>\n<p>SWE 類比: message queue 的 topic。解耦,發收方互不知。</p>\n<h3>目標: 建兩 node</h3>\n<ul><li>robot_news_station (publisher) — 每秒發一句話</li><li>smartphone (subscriber) — 收到就印</li></ul>\n<p>用同一個 my_py_pkg,加兩個新檔。</p>\n<h3>Step 1: Publisher node</h3>\n<p>新檔:ros2_ws/src/my_py_pkg/my_py_pkg/robot_news_station.py</p>\n<pre><code>import rclpy\nfrom rclpy.node import Node\nfrom example_interfaces.msg import String   # 內建字串訊息型別\n\n\nclass RobotNewsStation(Node):\n    def __init__(self):\n        super().__init__(\"robot_news_station\")\n        # 建 publisher: (訊息型別, topic名, queue大小)\n        self.publisher_ = self.create_publisher(String, \"robot_news\", 10)\n        self.create_timer(0.5, self.publish_news)   # 每 0.5 秒發一次\n        self.get_logger().info(\"Robot News Station started.\")\n\n    def publish_news(self):\n        msg = String()\n        msg.data = \"Hello from robot news station\"\n        self.publisher_.publish(msg)\n\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = RobotNewsStation()\n    try:\n        rclpy.spin(node)\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<p>新概念:</p>\n<ul><li>example_interfaces.msg.String = 標準訊息型別 (msg = 資料契約 = protobuf)</li><li>create_publisher(型別, \"topic名\", 10) — 10 = queue 大小 (滿了丟舊的)</li><li>發訊要先建 msg 物件,填 .data,再 publish()</li></ul>\n<h3>Step 2: Subscriber node</h3>\n<p>新檔:ros2_ws/src/my_py_pkg/my_py_pkg/smartphone.py</p>\n<pre><code>import rclpy\nfrom rclpy.node import Node\nfrom example_interfaces.msg import String\n\n\nclass Smartphone(Node):\n    def __init__(self):\n        super().__init__(\"smartphone\")\n        # 建 subscriber: (型別, topic名, callback, queue大小)\n        self.subscriber_ = self.create_subscription(\n            String, \"robot_news\", self.callback_news, 10)\n        self.get_logger().info(\"Smartphone started.\")\n\n    def callback_news(self, msg):\n        self.get_logger().info(f\"Received: {msg.data}\")   # 收到就觸發\n\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = Smartphone()\n    try:\n        rclpy.spin(node)\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<p>關鍵: publisher 用 timer 主動發; subscriber 無 timer,靠 callback 被動觸發 (收到才動)。event-driven。</p>\n<h3>Step 3: 註冊 entry points</h3>\n<p>改 ros2_ws/src/my_py_pkg/setup.py,找 entry_points 區塊,加兩行:</p>\n<pre><code>    entry_points={\n        'console_scripts': [\n            \"my_first_node = my_py_pkg.my_first_node:main\",\n            \"robot_news_station = my_py_pkg.robot_news_station:main\",\n            \"smartphone = my_py_pkg.smartphone:main\",\n        ],\n    },</code></pre>\n<ul><li>格式: \"可執行名 = package.檔名:函式\"</li><li>沒註冊 → ros2 run 找不到</li></ul>\n<h3>Step 4: 依賴宣告</h3>\n<p>改 ros2_ws/src/my_py_pkg/package.xml,在其他 &lt;depend&gt; 附近加:</p>\n<p>&lt;depend&gt;rclpy&lt;/depend&gt;&lt;depend&gt;example_interfaces&lt;/depend&gt;</p>\n<ul><li>宣告用到 example_interfaces (String 訊息來源)</li><li>好習慣,別漏</li></ul>\n<h3>Step 5: Build + 跑</h3>\n<p>cd /root/ros2_ws</p>\n<pre><code>colcon build\nsource install/setup.bash\n\n# Terminal 1 (publisher):\nsource install/setup.bash\nros2 run my_py_pkg robot_news_station\n\n# Terminal 2 (新開 docker exec -it ros2_dev bash, subscriber):\nsource /opt/ros/jazzy/setup.bash\nsource /root/ros2_ws/install/setup.bash\nros2 run my_py_pkg smartphone</code></pre>\n<p>預期: Terminal 2 每 0.5 秒印 Received: Hello from robot news station</p>\n<h3>Step 6: 用 CLI 檢視 (重要工具)</h3>\n<p>第三個 terminal:source /opt/ros/jazzy/setup.bashros2 topic list                    # 列所有 topic → 見 /robot_newsros2 topic echo /robot_news        # 直接偷看 topic 內容ros2 topic info /robot_news        # 看幾個 pub/subros2 topic hz /robot_news          # 發布頻率 (應 ~2Hz)</p>\n<p>ros2 topic echo = 神器,除錯必用 (不用寫 subscriber 就能看資料)。</p>\n<h3>驗收:</h3>\n<p>1. smartphone 持續收到訊息 ✅2. ros2 topic echo /robot_news 看得到 data ✅</p>\n<p>= Phase 1.2 過。ROS 精髓拿下。</p>"},{"id":"1/03-services","phase":1,"order":3,"slug":"03-services","title":"Services（同步請求/回應）","blurb":"用 request/response 模式做同步呼叫，server 收請求回結果，client 以 future 非同步取回應。","html":"<h1>Services（同步請求/回應）</h1>\n<blockquote>用 request/response 模式做同步呼叫，server 收請求回結果，client 以 future 非同步取回應。</blockquote>\n<h3>概念: Topic vs Service</h3>\n<pre><code>┌──────┬───────────────────┬────────────────────────┐\n│      │       Topic       │        Service         │\n├──────┼───────────────────┼────────────────────────┤\n│ 模式 │ pub/sub 串流      │ request/response       │\n├──────┼───────────────────┼────────────────────────┤\n│ 方向 │ 單向、多對多      │ 雙向、一對一           │\n├──────┼───────────────────┼────────────────────────┤\n│ 同步 │ 非同步 (發了不等) │ 同步 (等回應)          │\n├──────┼───────────────────┼────────────────────────┤\n│ 類比 │ Kafka / MQTT      │ REST / RPC             │\n├──────┼───────────────────┼────────────────────────┤\n│ 用途 │ sensor 連續資料   │ 觸發動作、查詢、算一次 │\n└──────┴───────────────────┴────────────────────────┘</code></pre>\n<p>判準: 要「連續流」用 Topic; 要「問一次拿答案」用 Service。</p>\n<p>[Client] --request--&gt; [Server][Client] &lt;--response-- [Server]</p>\n<p>Service interface = 兩塊: Request + Response (中間 --- 分隔)。</p>\n<h3>分兩段</h3>\n<ul><li>1.3A: 用內建 srv (AddTwoInts) 做 server + client</li><li>1.3B: 自訂 srv (需獨立 interface package — 重要概念)</li></ul>\n<h3>1.3A — Server + Client (內建 interface)</h3>\n<p>先看內建 srv 長相:ros2 interface show example_interfaces/srv/AddTwoInts預期:int64 aint64 b---int64 sum上半 = request (a, b),下半 = response (sum)。--- 分隔。</p>\n<h3>Step 1: Server node</h3>\n<p>新檔 ros2_ws/src/my_py_pkg/my_py_pkg/add_server.py:</p>\n<pre><code>import rclpy\nfrom rclpy.node import Node\nfrom example_interfaces.srv import AddTwoInts\n\n\nclass AddServer(Node):\n    def __init__(self):\n        super().__init__(\"add_server\")\n        # 建 service server: (srv型別, service名, callback)\n        self.server_ = self.create_service(\n            AddTwoInts, \"add_two_ints\", self.callback_add)\n        self.get_logger().info(\"Add server ready.\")\n\n    def callback_add(self, request, response):\n        response.sum = request.a + request.b        # 算\n        self.get_logger().info(\n            f\"{request.a} + {request.b} = {response.sum}\")\n        return response                              # 必須 return response\n\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = AddServer()\n    try:\n        rclpy.spin(node)\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<p>關鍵:</p>\n<ul><li>create_service(型別, \"名\", callback)</li><li>callback 收 request, 填 response, 必須 return response</li><li>收到請求才觸發 (event-driven,像 HTTP handler)</li></ul>\n<h3>Step 2: Client node</h3>\n<p>新檔 ros2_ws/src/my_py_pkg/my_py_pkg/add_client.py:</p>\n<pre><code>import rclpy\nfrom rclpy.node import Node\nfrom example_interfaces.srv import AddTwoInts\n\n\nclass AddClient(Node):\n    def __init__(self):\n        super().__init__(\"add_client\")\n        self.client_ = self.create_client(AddTwoInts, \"add_two_ints\")\n        # 等 server 上線 (server 沒開就等)\n        while not self.client_.wait_for_service(timeout_sec=1.0):\n            self.get_logger().info(\"Waiting for server...\")\n        self.send_request(3, 7)\n\n    def send_request(self, a, b):\n        request = AddTwoInts.Request()\n        request.a = a\n        request.b = b\n        # 非同步呼叫,拿 future\n        future = self.client_.call_async(request)\n        future.add_done_callback(self.callback_response)\n\n    def callback_response(self, future):\n        response = future.result()\n        self.get_logger().info(f\"Result: {response.sum}\")\n\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = AddClient()\n    try:\n        rclpy.spin(node)\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<p>新概念:</p>\n<ul><li>wait_for_service() — server 沒上線先等 (避免呼叫失敗)</li><li>call_async() → 回 future (= JS Promise)。非同步,不卡 event loop</li><li>future.add_done_callback() — 回應到了才觸發 (= .then())</li></ul>\n<p>▎ 為何非同步: ROS spin 是單執行緒 event loop,同步等會死鎖。future 模式標準。</p>\n<h3>Step 3: 註冊 entry points</h3>\n<p>setup.py 的 console_scripts 加:            \"add_server = my_py_pkg.add_server:main\",            \"add_client = my_py_pkg.add_client:main\",</p>\n<h3>Step 4: Build + 跑</h3>\n<pre><code>cd /root/ros2_ws\ncolcon build\nsource install/setup.bash\n\n# Terminal 1 (server):\nsource install/setup.bash\nros2 run my_py_pkg add_server\n\n# Terminal 2 (client):\nsource /opt/ros/jazzy/setup.bash\nsource /root/ros2_ws/install/setup.bash\nros2 run my_py_pkg add_client</code></pre>\n<p>預期: client 印 Result: 10,server 印 3 + 7 = 10</p>\n<h3>Step 5: CLI 呼叫 service (神器)</h3>\n<p>免寫 client,直接命令列打 server:</p>\n<pre><code>ros2 service list                  # 列 service → 見 /add_two_ints\nros2 service type /add_two_ints    # 看型別\nros2 service call /add_two_ints example_interfaces/srv/AddTwoInts \"{a: 5, b: 8}\"</code></pre>\n<p>預期: 回 sum=13。ros2 service call = 除錯 server 神器 (= curl 打 API)。</p>\n<h3>1.3A 驗收: client 得 10 + CLI call 得 13 = 過。</h3>"},{"id":"1/04-interfaces","phase":1,"order":4,"slug":"04-interfaces","title":"自訂 Interface（msg/srv）","blurb":"Python 無法自產 interface，需建獨立 ament_cmake package 集中 msg/srv 並生成跨語言型別。","html":"<h1>自訂 Interface（msg/srv）</h1>\n<blockquote>Python 無法自產 interface，需建獨立 ament_cmake package 集中 msg/srv 並生成跨語言型別。</blockquote>\n<h3>關鍵觀念 (先記)</h3>\n<blockquote><strong>Python package 不能自己產 interface。 必須建獨立的 ament_cmake package 專門放 msg/srv。</strong></blockquote>\n<p>原因: interface 靠 C++ 程式碼生成 (跨語言相容),Python 建置系統做不到。</p>\n<p>慣例: 一個專案配一個 xxx_interfaces package,集中所有自訂 msg/srv。</p>\n<p>my_interfaces/   ← ament_cmake, 只放 .msg/.srvmy_py_pkg/       ← ament_python, 你的 node,依賴上面</p>\n<h3>目標</h3>\n<p>建自訂 srv ComputeRectangleArea:</p>\n<ul><li>request: 長 + 寬</li><li>response: 面積 + 周長</li></ul>\n<h3>Step 1: 建 interface package</h3>\n<pre><code>cd /root/ros2_ws/src\nros2 pkg create my_interfaces --build-type ament_cmake</code></pre>\n<ul><li>注意: ament_cmake (不是 python!),因要產 interface</li><li>不加 --node-name (這 package 只放 interface,無 node)</li></ul>\n<h3>Step 2: 清掉沒用的資料夾,建 srv 夾</h3>\n<pre><code>cd /root/ros2_ws/src/my_interfaces\nrm -r include src\nmkdir srv</code></pre>\n<ul><li>ament_cmake 預設產 include/ src/ 放 C++ 碼,這 package 不用</li><li>srv/ 放 service 定義 (msg 放 msg/)</li></ul>\n<h3>Step 3: 寫 srv 檔</h3>\n<p>新檔 ros2_ws/src/my_interfaces/srv/ComputeRectangleArea.srv:</p>\n<pre><code>float64 length\nfloat64 width\n---\nfloat64 area\nfloat64 perimeter</code></pre>\n<ul><li>上半 request,---,下半 response</li><li>檔名必大寫開頭 (PascalCase),.srv 結尾。規定,錯了不編譯</li></ul>\n<h3>Step 4: 改 CMakeLists.txt</h3>\n<p>ros2_ws/src/my_interfaces/CMakeLists.txt,在 find_package(ament_cmake REQUIRED) 下面加:</p>\n<p>find_package(rosidl_default_generators REQUIRED)</p>\n<p>rosidl_generate_interfaces(${PROJECT_NAME}  \"srv/ComputeRectangleArea.srv\")</p>\n<p>ament_export_dependencies(rosidl_default_runtime)</p>\n<ul><li>rosidl_generate_interfaces = 產生 interface 的核心指令</li><li>每加一個 srv/msg,都要列進這清單</li></ul>\n<h3>Step 5: 改 package.xml</h3>\n<p>ros2_ws/src/my_interfaces/package.xml,在 </p>\n<pre><code>&lt;buildtool_depend&gt;ament_cmake&lt;/buildtool_depend&gt; 附近加:\n\n&lt;buildtool_depend&gt;rosidl_default_generators&lt;/buildtool_depend&gt;\n&lt;exec_depend&gt;rosidl_default_runtime&lt;/exec_depend&gt;\n&lt;member_of_group&gt;rosidl_interface_packages&lt;/member_of_group&gt;</code></pre>\n<h3>Step 6: Build interface package</h3>\n<pre><code>cd /root/ros2_ws\ncolcon build --packages-select my_interfaces\nsource install/setup.bash</code></pre>\n<ul><li>--packages-select = 只建這個 (省時)</li></ul>\n<p>驗證產生成功:ros2 interface show my_interfaces/srv/ComputeRectangleArea預期: 印出你定義的四個欄位 = 成功產生 🎯</p>\n<h3>Step 7: 用自訂 srv 寫 server</h3>\n<p>新檔 ros2_ws/src/my_py_pkg/my_py_pkg/area_server.py:</p>\n<pre><code>import rclpy\nfrom rclpy.node import Node\nfrom my_interfaces.srv import ComputeRectangleArea    # 你自訂的!\n\n\nclass AreaServer(Node):\n    def __init__(self):\n        super().__init__(\"area_server\")\n        self.server_ = self.create_service(\n            ComputeRectangleArea, \"compute_area\", self.callback_area)\n        self.get_logger().info(\"Area server ready.\")\n\n    def callback_area(self, request, response):\n        response.area = request.length * request.width\n        response.perimeter = 2 * (request.length + request.width)\n        self.get_logger().info(\n            f\"{request.length} x {request.width} \"\n            f\"-&gt; area={response.area}, perimeter={response.perimeter}\")\n        return response\n\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = AreaServer()\n    try:\n        rclpy.spin(node)\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<h3>Step 8: my_py_pkg 依賴 my_interfaces</h3>\n<p>ros2_ws/src/my_py_pkg/package.xml 加:&lt;depend&gt;my_interfaces&lt;/depend&gt;</p>\n<ul><li>宣告依賴自訂 interface package。漏了會編譯錯</li></ul>\n<p>setup.py 的 console_scripts 加:            \"area_server = my_py_pkg.area_server:main\",</p>\n<h3>Step 9: Build 全部 + 跑</h3>\n<pre><code>cd /root/ros2_ws\ncolcon build\nsource install/setup.bash\n\n# Terminal 1:\nsource install/setup.bash\nros2 run my_py_pkg area_server\n\n# Terminal 2 (CLI 打,免寫 client):\nsource /opt/ros/jazzy/setup.bash\nsource /root/ros2_ws/install/setup.bash\nros2 service call /compute_area my_interfaces/srv/ComputeRectangleArea \"{length: 4.0, width: 3.0}\"</code></pre>\n<p>預期: 回 area=12.0, perimeter=14.0,server 印算式</p>\n<h3>驗收</h3>\n<p>1. ros2 interface show my_interfaces/srv/ComputeRectangleArea 印出欄位 ✅2. service call 回 area=12, perimeter=14 ✅</p>\n<p>= Phase 1.3 完整過。你會建+用自訂 interface = ROS 2 分水嶺技能之一。</p>\n<h3>常見坑</h3>\n<pre><code>┌───────────────────────────┬─────────────────────────────────────────────┐\n│           症狀            │                    原因                     │\n├───────────────────────────┼─────────────────────────────────────────────┤\n│ interface show 找不到     │ build 沒過 / 沒 source                      │\n├───────────────────────────┼─────────────────────────────────────────────┤\n│ import my_interfaces 失敗 │ my_py_pkg 漏 &lt;depend&gt;my_interfaces&lt;/depend&gt; │\n├───────────────────────────┼─────────────────────────────────────────────┤\n│ srv 不生成                │ CMakeLists 沒列檔名 / package.xml 缺 rosidl │\n├───────────────────────────┼─────────────────────────────────────────────┤\n│ 檔名報錯                  │ srv 檔名非 PascalCase                       │\n└───────────────────────────┴─────────────────────────────────────────────┘</code></pre>"},{"id":"1/05-parameters-launch","phase":1,"order":5,"slug":"05-parameters-launch","title":"Parameters + Launch（工程化）","blurb":"runtime 調參讓同一 node 展現不同行為，再用 launch 檔一鍵編排多節點。","html":"<h1>Parameters + Launch（工程化）</h1>\n<blockquote>runtime 調參讓同一 node 展現不同行為，再用 launch 檔一鍵編排多節點。</blockquote>\n<p>Phase 1 收尾。兩主題: runtime 調參 + 多節點編排。</p>\n<h3>Part A — Parameters</h3>\n<h3>概念</h3>\n<p>Parameter = node 的 runtime 設定 (= env var / feature flag / config)。</p>\n<ul><li>啟動時給值,免改碼</li><li>同一 node,不同參數 → 不同行為 (可重用)</li></ul>\n<p>類比: docker run -e KEY=val / CLI flag。</p>\n<h3>改造 robot_news_station 加參數</h3>\n<p>讓「電台名」「發布頻率」可調。改 robot_news_station.py:</p>\n<pre><code>import rclpy\nfrom rclpy.node import Node\nfrom example_interfaces.msg import String\n\n\nclass RobotNewsStation(Node):\n    def __init__(self):\n        super().__init__(\"robot_news_station\")\n\n        # 宣告參數 + 預設值\n        self.declare_parameter(\"station_name\", \"default_station\")\n        self.declare_parameter(\"publish_frequency\", 2.0)\n\n        # 讀參數值\n        self.station_name_ = self.get_parameter(\"station_name\").value\n        freq = self.get_parameter(\"publish_frequency\").value\n\n        self.publisher_ = self.create_publisher(String, \"robot_news\", 10)\n        self.create_timer(1.0 / freq, self.publish_news)   # 頻率 → 週期\n        self.get_logger().info(f\"{self.station_name_} started at {freq} Hz.\")\n\n    def publish_news(self):\n        msg = String()\n        msg.data = f\"Hello from {self.station_name_}\"\n        self.publisher_.publish(msg)\n\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = RobotNewsStation()\n    try:\n        rclpy.spin(node)\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<p>三步: declare_parameter(名, 預設) → get_parameter(名).value 讀 → 用。</p>\n<h3>Build + 跑 (帶參數)</h3>\n<pre><code>cd /root/ros2_ws\ncolcon build --packages-select my_py_pkg\nsource install/setup.bash</code></pre>\n<p>預設跑:</p>\n<pre><code>ros2 run my_py_pkg robot_news_station</code></pre>\n<p>預期: default_station started at 2.0 Hz.</p>\n<p>帶參數跑 (--ros-args -p 名:=值):</p>\n<pre><code>ros2 run my_py_pkg robot_news_station --ros-args -p station_name:=BBC -p publish_frequency:=5.0</code></pre>\n<p>預期: BBC started at 5.0 Hz. — 同碼,不同行為 🎯</p>\n<p>驗證頻率:</p>\n<pre><code>ros2 topic hz /robot_news   # 應 ~5Hz</code></pre>\n<h3>Part B — Launch files</h3>\n<h3>概念</h3>\n<p>Launch = 一鍵起多 node + 設參數 (= docker-compose / k8s manifest)。</p>\n<ul><li>手動一個個 ros2 run 太累</li><li>launch 檔宣告: 起哪些 node、給什麼參數、remap 什麼</li></ul>\n<h3>慣例: 獨立 launch package</h3>\n<p>Launch 檔通常放專門的 xxx_bringup package。先建:</p>\n<pre><code>cd /root/ros2_ws/src\nros2 pkg create my_bringup --build-type ament_cmake\ncd my_bringup\nrm -r include src\nmkdir launch</code></pre>\n<p>改 my_bringup/CMakeLists.txt,find_package(ament_cmake REQUIRED) 下加:install(DIRECTORY  launch  DESTINATION share/${PROJECT_NAME})</p>\n<ul><li>讓 launch 檔被安裝進 install (才找得到)</li></ul>\n<h3>寫 launch 檔</h3>\n<p>新檔 ros2_ws/src/my_bringup/launch/news_system.launch.py:</p>\n<pre><code>from launch import LaunchDescription\nfrom launch_ros.actions import Node\n\n\ndef generate_launch_description():\n    ld = LaunchDescription()\n\n    # publisher node,帶參數\n    station = Node(\n        package=\"my_py_pkg\",\n        executable=\"robot_news_station\",\n        parameters=[\n            {\"station_name\": \"CNN\"},\n            {\"publish_frequency\": 3.0},\n        ]\n    )\n\n    # subscriber node\n    phone = Node(\n        package=\"my_py_pkg\",\n        executable=\"smartphone\",\n    )\n\n    ld.add_action(station)\n    ld.add_action(phone)\n    return ld</code></pre>\n<p>結構:</p>\n<ul><li>Node(package, executable, parameters=[...]) = 描述一個要起的 node</li><li>parameters 用 dict list 設參數</li><li>add_action 加進 launch 描述</li></ul>\n<h3>Build + 跑</h3>\n<p>cd /root/ros2_wscolcon build --packages-select my_bringupsource install/setup.bashros2 launch my_bringup news_system.launch.py</p>\n<p>預期: 一個指令同時起 station (CNN, 3Hz) + smartphone,兩者 log 交錯出現:[robot_news_station-1] [INFO] ... CNN started at 3.0 Hz.[smartphone-2] [INFO] ... Received: Hello from CNN[smartphone-2] [INFO] ... Received: Hello from CNN</p>\n<p>檢視:ros2 node list      # 另一 terminal → 見兩 node 都活著</p>\n<h3>Phase 1.4 驗收</h3>\n<p>1. -p 帶參數改變 node 行為 ✅2. ros2 launch 一鍵起兩 node,參數生效 ✅</p>\n<h3>🏁 Phase 1 大專案 (capstone)</h3>\n<p>整合所學,自己做 (查前面碼,別複製):</p>\n<p>做一個 number 系統:1. number_publisher node — 每秒發一個數字 (Int64)。數字用 parameter 設 (預設 2)2. number_counter node — 訂數字,累加,把總和發到另一 topic /number_count3. number_counter 加一個 service /reset_counter — 呼叫時歸零 (用內建 example_interfaces/srv/SetBool)4. 寫 launch 檔一鍵起兩 node,number_publisher 參數設 5</p>\n<p>用到: Topic (pub+sub) + Parameter + Service + Launch = Phase 1 全部。</p>"},{"id":"2/01-actions","phase":2,"order":1,"slug":"01-actions","title":"Actions（長任務通訊）","blurb":"用 Action 處理耗時任務：送目標、持續回報進度、可中途取消，補足 Topic 與 Service 的不足。","html":"<h1>Actions（長任務通訊）</h1>\n<blockquote>用 Action 處理耗時任務：送目標、持續回報進度、可中途取消，補足 Topic 與 Service 的不足。</blockquote>\n<h3>概念：三種通訊比較</h3>\n<pre><code>┌──────┬────────┬──────────────┬────────────────────────────────┐\n│      │ Topic  │   Service    │             Action             │\n├──────┼────────┼──────────────┼────────────────────────────────┤\n│ 模式 │ 串流   │ 一次請求回應 │ 長任務 + 回饋 + 可取消         │\n├──────┼────────┼──────────────┼────────────────────────────────┤\n│ 同步 │ 非同步 │ 同步         │ 非同步                         │\n├──────┼────────┼──────────────┼────────────────────────────────┤\n│ 回饋 │ 無     │ 無           │ 執行中持續回報進度             │\n├──────┼────────┼──────────────┼────────────────────────────────┤\n│ 取消 │ —      │ 不可         │ 可中途取消                     │\n├──────┼────────┼──────────────┼────────────────────────────────┤\n│ 類比 │ Kafka  │ REST         │ 長 async job + 進度條 + 取消鈕 │\n└──────┴────────┴──────────────┴────────────────────────────────┘</code></pre>\n<p>何時用 Action: 任務耗時且要追進度/可取消。例: \"移動到座標X\" (走 10 秒)、\"導航到房間\"、\"手臂抓取\"。</p>\n<p>Service 不行 (會卡死等太久,不能取消)。Topic 不行 (無回應無結構)。</p>\n<h3>Action 三部分</h3>\n<pre><code>Goal     → client 送目標 (如: 數到 10)\nFeedback → server 執行中持續回報 (現在數到 3... 4... 5)\nResult   → 完成回最終結果 (數完了, 總和 55)</code></pre>\n<h3>目標：CountUntil action</h3>\n<ul><li>Goal: 數到幾 (target) + 每次間隔 (period)</li><li>Feedback: 目前數到幾</li><li>Result: 最終數字</li></ul>\n<h3>Step 1: 定義 action interface</h3>\n<p>Action 也要在 ament_cmake package (同 srv)。用現有 my_interfaces。</p>\n<p>建 action 夾:</p>\n<pre><code>mkdir /root/ros2_ws/src/my_interfaces/action</code></pre>\n<p>新檔 ros2_ws/src/my_interfaces/action/CountUntil.action:</p>\n<pre><code># Goal\nint64 target_number\nfloat64 period\n---\n# Result\nint64 reached_number\n---\n# Feedback\nint64 current_number</code></pre>\n<p>三段,兩個 --- 分隔 (比 srv 多一段)。順序固定: Goal / Result / Feedback。</p>\n<h3>Step 2: 註冊到 CMakeLists</h3>\n<p>my_interfaces/CMakeLists.txt 的 rosidl_generate_interfaces 加 action 行:</p>\n<pre><code>rosidl_generate_interfaces(${PROJECT_NAME}\n  \"srv/ComputeRectangleArea.srv\"\n  \"action/CountUntil.action\"\n)</code></pre>\n<p>package.xml 已有 rosidl 依賴 (1.3B 設過),不用改。</p>\n<h3>Step 3: Build interface</h3>\n<pre><code>cd /root/ros2_ws\ncolcon build --packages-select my_interfaces\nsource install/setup.bash\nros2 interface show my_interfaces/action/CountUntil</code></pre>\n<p>預期: 印出三段定義 = action interface 生成成功 🎯</p>\n<h3>Step 4: Action Server</h3>\n<p>新檔 ros2_ws/src/my_py_pkg/my_py_pkg/count_until_server.py:</p>\n<pre><code>import time\nimport rclpy\nfrom rclpy.node import Node\nfrom rclpy.action import ActionServer\nfrom my_interfaces.action import CountUntil\n\n\nclass CountUntilServer(Node):\n    def __init__(self):\n        super().__init__(\"count_until_server\")\n        self.server_ = ActionServer(\n            self,\n            CountUntil,\n            \"count_until\",\n            execute_callback=self.execute_callback)\n        self.get_logger().info(\"Count Until server started.\")\n\n    def execute_callback(self, goal_handle):\n        # 讀 goal\n        target = goal_handle.request.target_number\n        period = goal_handle.request.period\n        self.get_logger().info(f\"Counting to {target}\")\n\n        # 執行 + 持續發 feedback\n        feedback = CountUntil.Feedback()\n        result = CountUntil.Result()\n        counter = 0\n        for i in range(target):\n            counter += 1\n            self.get_logger().info(f\"  {counter}\")\n            feedback.current_number = counter\n            goal_handle.publish_feedback(feedback)     # 發進度\n            time.sleep(period)\n\n        # 完成\n        goal_handle.succeed()                          # 標記成功\n        result.reached_number = counter\n        return result\n\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = CountUntilServer()\n    try:\n        rclpy.spin(node)\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<p>新概念:</p>\n<ul><li>ActionServer(node, 型別, \"名\", execute_callback)</li><li>goal_handle.request.xxx — 讀 goal 欄位</li><li>goal_handle.publish_feedback(fb) — 執行中發進度</li><li>goal_handle.succeed() — 標記完成</li><li>return result — 回最終結果</li></ul>\n<p>▎ 註: 這版簡化 (沒處理取消)。取消邏輯 2.1 後補。</p>\n<h3>Step 5: 註冊 + build</h3>\n<p>setup.py console_scripts 加:            \"count_until_server = my_py_pkg.count_until_server:main\",</p>\n<pre><code>cd /root/ros2_ws\ncolcon build --packages-select my_py_pkg\nsource install/setup.bash</code></pre>\n<h3>Step 6: 跑 + CLI 測 (免寫 client)</h3>\n<pre><code># Terminal 1:\nsource install/setup.bash\nros2 run my_py_pkg count_until_server\n\n# Terminal 2 — CLI 送 goal + 看 feedback:\nsource /opt/ros/jazzy/setup.bash\nsource /root/ros2_ws/install/setup.bash\nros2 action send_goal /count_until my_interfaces/action/CountUntil \"{target_number: 5, period: 1.0}\" --feedback</code></pre>\n<p>--feedback = 顯示執行中回饋。</p>\n<p>預期: Terminal 2 每秒印 feedback current_number: 1, 2, 3...,最後 result: reached_number=5。Terminal 1 印計數。</p>\n<p>檢視工具:ros2 action listros2 action info /count_until</p>\n<h3>Phase 2.1 驗收</h3>\n<p>1. ros2 interface show 印出 action 三段 ✅2. send_goal --feedback 看到進度串流 + 最終 result ✅</p>\n<p>= Action 基本拿下 (ROS 2 最複雜通訊型別)。</p>"},{"id":"2/02-action-client-cancel","phase":2,"order":2,"slug":"02-action-client-cancel","title":"Action Client + 取消","blurb":"寫 Python action client 收 feedback/result，並用 executor 與 callback group 讓長任務可被中途取消。","html":"<h1>Action Client + 取消</h1>\n<blockquote>寫 Python action client 收 feedback/result，並用 executor 與 callback group 讓長任務可被中途取消。</blockquote>\n<h3>先補 Server: 支援取消</h3>\n<p>現在 server 不能取消。加取消處理。改 count_until_server.py:</p>\n<pre><code>import time\nimport rclpy\nfrom rclpy.node import Node\nfrom rclpy.action import ActionServer\nfrom rclpy.action.server import ServerGoalHandle\nfrom rclpy.callback_groups import ReentrantCallbackGroup\nfrom my_interfaces.action import CountUntil\n\n\nclass CountUntilServer(Node):\n    def __init__(self):\n        super().__init__(\"count_until_server\")\n        self.server_ = ActionServer(\n            self,\n            CountUntil,\n            \"count_until\",\n            execute_callback=self.execute_callback,\n            cancel_callback=self.cancel_callback,          # 新增\n            callback_group=ReentrantCallbackGroup())       # 新增\n        self.get_logger().info(\"Count Until server started.\")\n\n    def cancel_callback(self, goal_handle: ServerGoalHandle):\n        self.get_logger().info(\"Cancel request received.\")\n        from rclpy.action import CancelResponse\n        return CancelResponse.ACCEPT                        # 接受取消\n\n    def execute_callback(self, goal_handle: ServerGoalHandle):\n        target = goal_handle.request.target_number\n        period = goal_handle.request.period\n        self.get_logger().info(f\"Counting to {target}\")\n\n        feedback = CountUntil.Feedback()\n        result = CountUntil.Result()\n        counter = 0\n        for i in range(target):\n            # 每圈檢查是否被要求取消\n            if goal_handle.is_cancel_requested:\n                self.get_logger().info(\"Goal canceled.\")\n                goal_handle.canceled()\n                result.reached_number = counter\n                return result\n\n            counter += 1\n            self.get_logger().info(f\"  {counter}\")\n            feedback.current_number = counter\n            goal_handle.publish_feedback(feedback)\n            time.sleep(period)\n\n        goal_handle.succeed()\n        result.reached_number = counter\n        return result</code></pre>\n<p>取消三要素:</p>\n<ul><li>cancel_callback 回 CancelResponse.ACCEPT — 同意可取消</li><li>執行迴圈每圈查 goal_handle.is_cancel_requested</li><li>被取消 → goal_handle.canceled() + return</li></ul>\n<h3>2.2 插播: 為何要 ReentrantCallbackGroup</h3>\n<p>問題: server 執行 time.sleep 卡住迴圈時,取消請求怎麼被聽到?</p>\n<p>答: 靠 executor + callback group。</p>\n<p>Executor = ROS 的 callback 調度器</p>\n<ul><li>rclpy.spin(node) 底層 = SingleThreadedExecutor (單執行緒)</li><li>單執行緒 = callback 一次跑一個,前一個沒跑完後面排隊</li><li>execute_callback 在 sleep → 若同群,cancel_callback 排不進來 → 取消失效</li></ul>\n<p>Callback Group = 併發規則</p>\n<pre><code>┌───────────────────────────────────────┬───────────────────────────────┐\n│                 型別                  │             行為              │\n├───────────────────────────────────────┼───────────────────────────────┤\n│ MutuallyExclusiveCallbackGroup (預設) │ 同群 callback 不併發,一個個來 │\n├───────────────────────────────────────┼───────────────────────────────┤\n│ ReentrantCallbackGroup                │ 同群 callback 可併發          │\n└───────────────────────────────────────┴───────────────────────────────┘</code></pre>\n<p>解: action server 用 ReentrantCallbackGroup → execute 跑時,cancel 能同時插入 → 取消聽得到。</p>\n<p>但 Reentrant 需要多執行緒 executor 才真併發。</p>\n<p>類比 (你 SWE):</p>\n<ul><li>Executor = event loop / thread pool</li><li>SingleThreaded = 單執行緒 event loop (Node.js)</li><li>MultiThreaded = thread pool</li><li>Callback group = 併發策略 (哪些能同時跑)</li></ul>\n<h3>改 main: 用 MultiThreadedExecutor</h3>\n<p>count_until_server.py 的 main 改:</p>\n<pre><code>from rclpy.executors import MultiThreadedExecutor\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = CountUntilServer()\n    executor = MultiThreadedExecutor()          # 多執行緒\n    executor.add_node(node)\n    try:\n        executor.spin()                         # 用 executor 而非 rclpy.spin\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()</code></pre>\n<p>心智: 要併發處理 (執行中還能收取消/其他請求) → MultiThreadedExecutor + ReentrantCallbackGroup。單純 node 不需要,預設 spin 就好。</p>\n<h3>Step: Action Client (Python)</h3>\n<p>新檔 ros2_ws/src/my_py_pkg/my_py_pkg/count_until_client.py:</p>\n<pre><code>import rclpy\nfrom rclpy.node import Node\nfrom rclpy.action import ActionClient\nfrom my_interfaces.action import CountUntil\n\n\nclass CountUntilClient(Node):\n    def __init__(self):\n        super().__init__(\"count_until_client\")\n        self.client_ = ActionClient(self, CountUntil, \"count_until\")\n\n    def send_goal(self, target, period):\n        self.client_.wait_for_server()                  # 等 server\n        goal = CountUntil.Goal()\n        goal.target_number = target\n        goal.period = period\n        self.get_logger().info(\"Sending goal...\")\n        # 送 goal + 註冊 feedback callback\n        self.client_.send_goal_async(\n            goal, feedback_callback=self.feedback_callback\n        ).add_done_callback(self.goal_response_callback)\n\n    def goal_response_callback(self, future):\n        self.goal_handle_ = future.result()\n        if not self.goal_handle_.accepted:\n            self.get_logger().warn(\"Goal rejected.\")\n            return\n        self.get_logger().info(\"Goal accepted.\")\n        # 拿最終結果\n        self.goal_handle_.get_result_async().add_done_callback(\n            self.result_callback)\n\n    def feedback_callback(self, feedback_msg):\n        current = feedback_msg.feedback.current_number\n        self.get_logger().info(f\"Feedback: {current}\")\n\n    def result_callback(self, future):\n        result = future.result().result\n        self.get_logger().info(f\"Result: {result.reached_number}\")\n\n\ndef main(args=None):\n    rclpy.init(args=args)\n    node = CountUntilClient()\n    node.send_goal(6, 1.0)\n    try:\n        rclpy.spin(node)\n    except KeyboardInterrupt:\n        pass\n    finally:\n        node.destroy_node()\n        rclpy.shutdown()\n\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n<p>Client 流程 (三層 future,event-driven):1. send_goal_async → server 接受/拒絕 (goal_response_callback)2. feedback_callback — 執行中持續收進度3. get_result_async → 最終結果 (result_callback)</p>\n<p>比 service client 多層 (goal接受 → feedback → result)。這是 action 複雜處。</p>\n<h3>Build + 跑</h3>\n<p>setup.py 加:            \"count_until_client = my_py_pkg.count_until_client:main\",</p>\n<pre><code>cd /root/ros2_ws\ncolcon build --packages-select my_py_pkg\nsource install/setup.bash\n\n# Terminal 1:\nsource install/setup.bash\nros2 run my_py_pkg count_until_server\n\n# Terminal 2:\nsource /opt/ros/jazzy/setup.bash\nsource /root/ros2_ws/install/setup.bash\nros2 run my_py_pkg count_until_client</code></pre>\n<p>預期: client 印 Goal accepted → Feedback: 1..6 → Result: 6</p>\n<h3>測取消 (CLI 最快)</h3>\n<p>server 開著,Terminal 2 送長 goal 然後 Ctrl+C:</p>\n<pre><code>ros2 action send_goal /count_until my_interfaces/action/CountUntil \"{target_number: 100, period: 0.5}\" --feedback</code></pre>\n<p>數到中途按 Ctrl+C → server 應印 Cancel request received + Goal canceled。取消生效 = executor/callback group 起作用。</p>\n<h3>驗收</h3>\n<p>1. Python client 收 feedback + result ✅2. 中途 Ctrl+C → server 印 canceled ✅</p>\n<p>= Action 完整 (server/client/feedback/cancel) + 懂 executor/callback group。Phase 2 最硬部分過。</p>\n<h3>記憶點</h3>\n<pre><code>單純 node        → rclpy.spin (單執行緒足夠)\n要併發/取消/多請求 → MultiThreadedExecutor + ReentrantCallbackGroup</code></pre>"},{"id":"2/03-qos","phase":2,"order":3,"slug":"03-qos","title":"QoS（通訊品質）","blurb":"QoS 決定 topic 訊息要保證送達還是求快可丟；pub/sub 政策不相容會靜默收不到，是真機除錯最常見的雷。","html":"<h1>QoS（通訊品質）</h1>\n<blockquote>QoS 決定 topic 訊息要保證送達還是求快可丟；pub/sub 政策不相容會靜默收不到，是真機除錯最常見的雷。</blockquote>\n<h3>概念</h3>\n<blockquote>QoS = Quality of Service = topic 通訊的可靠性/時效策略。</blockquote>\n<p>ROS 2 底層走 DDS (Data Distribution Service,工業級 pub/sub 中介)。DDS 讓你調: 訊息要保證送達還是求快可丟?</p>\n<p>類比 (你 SWE): TCP (可靠, 重傳) vs UDP (快, 可丟)。QoS 就是 per-topic 選這個。</p>\n<p>關鍵坑: publisher 跟 subscriber 的 QoS 不相容 → 收不到訊息。無錯誤,靜默失敗。ROS 2 新手最陰的雷。</p>\n<h3>五個主要 QoS 政策</h3>\n<pre><code>┌─────────────────────┬────────────────────────────┬──────────────────────────────────┐\n│        政策         │            選項            │               意義               │\n├─────────────────────┼────────────────────────────┼──────────────────────────────────┤\n│ Reliability         │ RELIABLE / BEST_EFFORT     │ 保證送達(重傳) vs 求快可丟       │\n├─────────────────────┼────────────────────────────┼──────────────────────────────────┤\n│ Durability          │ TRANSIENT_LOCAL / VOLATILE │ 保留給晚加入者 vs 只給當下在線的 │\n├─────────────────────┼────────────────────────────┼──────────────────────────────────┤\n│ History             │ KEEP_LAST(n) / KEEP_ALL    │ 留最近 n 筆 vs 全留              │\n├─────────────────────┼────────────────────────────┼──────────────────────────────────┤\n│ Depth               │ 數字                       │ queue 大小 (搭 KEEP_LAST)        │\n├─────────────────────┼────────────────────────────┼──────────────────────────────────┤\n│ Deadline / Lifespan │ 時間                       │ 進階,先略                        │\n└─────────────────────┴────────────────────────────┴──────────────────────────────────┘</code></pre>\n<h3>實務對照 (何時用什麼)</h3>\n<pre><code>┌──────────────────┬────────────────────────────┬──────────────────────────────┐\n│     資料型別     │        Reliability         │             原因             │\n├──────────────────┼────────────────────────────┼──────────────────────────────┤\n│ 相機影像 / LiDAR │ BEST_EFFORT                │ 量大高頻,丟幾張無所謂,求即時 │\n├──────────────────┼────────────────────────────┼──────────────────────────────┤\n│ 控制指令 / 目標  │ RELIABLE                   │ 不能丟,漏了機器人亂動        │\n├──────────────────┼────────────────────────────┼──────────────────────────────┤\n│ 地圖 / 靜態設定  │ RELIABLE + TRANSIENT_LOCAL │ 晚上線的 node 也要拿到最後值 │\n└──────────────────┴────────────────────────────┴──────────────────────────────┘</code></pre>\n<p>心智: 高頻可丟 → best_effort;關鍵不可丟 → reliable;要\"最後已知值\" → transient_local。</p>\n<h3>動手: 看 QoS + 製造不相容</h3>\n<p>1. 看現有 topic 的 QoS</p>\n<p>server 開著 (robot_news_station 或 count 都行),或起個 publisher。查:ros2 topic info /robot_news --verbose預期: 印出 publisher 的 QoS profile — Reliability: RELIABLE, Durability: VOLATILE 等。</p>\n<pre><code>QoS profile:\n  Reliability: RELIABLE\n  History (Depth): UNKNOWN\n  Durability: VOLATILE\n  Lifespan: Infinite\n  Deadline: Infinite\n  Liveliness: AUTOMATIC\n  Liveliness lease duration: Infinite</code></pre>\n<p>2. 程式碼設 QoS</p>\n<p>改 robot_news_station.py 的 publisher,用自訂 QoS:</p>\n<p>from rclpy.qos import QoSProfile, ReliabilityPolicy, DurabilityPolicy, HistoryPolicy</p>\n<pre><code># ... 在 __init__ 內:\n        qos = QoSProfile(\n            reliability=ReliabilityPolicy.BEST_EFFORT,\n            durability=DurabilityPolicy.VOLATILE,\n            history=HistoryPolicy.KEEP_LAST,\n            depth=10,\n        )\n        self.publisher_ = self.create_publisher(String, \"robot_news\", qos)</code></pre>\n<ul><li>第三參數從 10 改成 qos 物件 (數字其實是 depth 的簡寫)</li></ul>\n<pre><code>subscriber (smartphone.py) 也要設相容 QoS:\nfrom rclpy.qos import QoSProfile, ReliabilityPolicy\n\n        qos = QoSProfile(reliability=ReliabilityPolicy.BEST_EFFORT, depth=10)\n        self.subscriber_ = self.create_subscription(\n            String, \"robot_news\", self.callback_news, qos)</code></pre>\n<p>3. Build + 跑相容版</p>\n<p>cd /root/ros2_wscolcon build --packages-select my_py_pkgsource install/setup.bash兩 terminal 跑 station + smartphone → 正常收到 (兩邊都 BEST_EFFORT)。</p>\n<p>4. 製造不相容 (看靜默失敗)</p>\n<p>把 subscriber 改回 RELIABLE:        qos = QoSProfile(reliability=ReliabilityPolicy.RELIABLE, depth=10)publisher 留 BEST_EFFORT。rebuild,兩邊跑。</p>\n<p>預期: smartphone 收不到任何訊息,無錯誤訊息。</p>\n<p>原因: RELIABLE subscriber 不接受 BEST_EFFORT publisher (要求比對方高,不相容)。</p>\n<p>▎ 相容規則: subscriber 要求 ≤ publisher 提供。RELIABLE sub 要 RELIABLE pub;BEST_EFFORT sub 可收兩者。</p>\n<p>5. 診斷工具</p>\n<p>ros2 topic info /robot_news --verbose看兩邊 QoS 對不對得上。以後 topic 靜默沒資料,先查這個。</p>\n<h3>驗收</h3>\n<p>1. topic info --verbose 看得到 QoS profile ✅2. 兩邊 BEST_EFFORT → 通;pub best_effort + sub reliable → 靜默失敗 ✅3. 懂為何失敗 (相容規則) ✅</p>\n<p>= QoS 拿下。這是真機除錯最常卡的雷 (sensor 收不到 9 成是 QoS)。</p>"},{"id":"2/04-ros2-bag","phase":2,"order":4,"slug":"04-ros2-bag","title":"ros2 bag（錄製與回放）","blurb":"用 ros2 bag 錄下 topic 資料存檔再回放，讓你免開真機就能離線重跑、除錯與比較演算法。","html":"<h1>ros2 bag（錄製與回放）</h1>\n<blockquote>用 ros2 bag 錄下 topic 資料存檔再回放，讓你免開真機就能離線重跑、除錯與比較演算法。</blockquote>\n<h3>概念</h3>\n<p>ros2 bag = 錄 topic 資料存檔,之後回放 (= tcpdump / DVR / 錄影)。</p>\n<p>用途 (真機開發命脈):</p>\n<ul><li>錄真機 sensor 資料 → 之後離線重跑演算法,免每次開機器人</li><li>Debug: 錄下出錯瞬間,反覆回放分析</li><li>測試: 同一份錄製資料餵不同版本演算法,公平比較</li><li>對應中文課「機械臂錄製+回放」(塊2)</li></ul>\n<p>存成 SQLite/MCAP 檔,含訊息 + 時間戳 (回放保留原時序)。</p>\n<h3>Step 1: 準備 — 起一個 publisher</h3>\n<p>Terminal 1:source /opt/ros/jazzy/setup.bashsource /root/ros2_ws/install/setup.bashros2 run my_py_pkg robot_news_station持續發 /robot_news。</p>\n<h3>Step 2: 錄製</h3>\n<pre><code>Terminal 2:\nsource /opt/ros/jazzy/setup.bash\ncd /root/ros2_ws   # 錄到掛載目錄,檔案留 Mac 上\n# 錄單一 topic\nros2 bag record /robot_news</code></pre>\n<p>預期: Recording...,建出 rosbag2_YYYY_MM_DD-HH_MM_SS/ 資料夾。</p>\n<p>錄幾秒 → Ctrl+C 停。</p>\n<p>這就是威力: 錄一次真機資料 → 之後無限次餵給任何 node 開發測試,不用真機。sim-to-real 開發的日常工具。</p>\n<p>其他錄法:</p>\n<pre><code>ros2 bag record /robot_news /number_count      # 多 topic\nros2 bag record -a                             # 全部 topic\nros2 bag record -o my_recording /robot_news    # 指定輸出名</code></pre>\n<h3>Step 3: 檢視錄製內容</h3>\n<p>ros2 bag info rosbag2_2026_07_02-XX_XX_XX(換成你實際夾名,ls 可看)</p>\n<p>預期: 印出:</p>\n<ul><li>Duration (時長)</li><li>Messages (訊息數)</li><li>Topic + 型別 + count</li><li>Storage (mcap/sqlite3)</li></ul>\n<p>= 不用回放就知道錄了啥。</p>\n<h3>Step 4: 回放</h3>\n<p>先關掉 Terminal 1 的 publisher (Ctrl+C) — 證明資料來自 bag 非真 publisher。</p>\n<p>Terminal 2 回放:ros2 bag play rosbag2_2026_07_02-XX_XX_XX</p>\n<p>Terminal 3 (新開) 監看:source /opt/ros/jazzy/setup.bashros2 topic echo /robot_news</p>\n<p>預期: publisher 已關,但 echo 仍收到訊息 — 來自 bag 回放。時序跟原本一致 🎯</p>\n<p>回放選項:ros2 bag play &lt;bag&gt; --rate 2.0      # 2 倍速ros2 bag play &lt;bag&gt; --loop          # 循環播ros2 bag play &lt;bag&gt; --rate 0.5</p>\n<h3>驗收</h3>\n<p>1. ros2 bag record 建出 bag 夾 ✅2. ros2 bag info 看得到 topic/訊息數 ✅3. publisher 關掉,ros2 bag play 仍能讓 subscriber 收到 ✅</p>\n<p>= ros2 bag 拿下。</p>\n<h3>.gitignore 注意</h3>\n<p>bag 檔可能大 (sensor 影像更大)。建議加:</p>\n<pre><code>ros2_ws/*.db3\nros2_ws/*.mcap\nrosbag2_*/</code></pre>\n<p>避免誤 commit 大檔。</p>"}];
window.STATS = {"total":13,"complete":5,"phases":7,"docs":10};
